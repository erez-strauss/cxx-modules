import tally.log;

int main()
{
    // This file is NOT part of the default build. It is the live demo of
    // the diagnostic you get when a team expects #include-style macros.
    TALLY_TRACE("this will not compile");
    return 0;
}
